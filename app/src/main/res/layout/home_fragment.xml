<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/whale_portrait">


<!--app:srcCompat="@drawable/whale_portrait"-->
    <!--android:src="@drawable/whale_portrait"-->

    <!--android:scaleType = "centerCrop"/>-->

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:gravity="bottom|center_horizontal"
        android:orientation="horizontal"
        android:background="@color/background_dark"
        android:alpha="0.6"
        android:weightSum="1">

        <!--flat:-->
        <!--style="?android:attr/borderlessButtonStyle"-->
        <Button
            android:layout_height="wrap_content"
            android:layout_width="0dp"
            style="?android:attr/borderlessButtonStyle"
            android:id="@+id/button_launch_browse_species"
            android:text="Browse Species"
            android:theme="@style/IntroButtons"
            android:layout_weight=".5"/>

        <View
            android:layout_width="5dp"
            android:layout_height="match_parent"
            android:background="?android:attr/listDivider"
            />

        <Button
            android:layout_height="wrap_content"
            android:layout_width="0dp"
            style="?android:attr/borderlessButtonStyle"
            android:id="@+id/button_launch_select_sites"
            android:theme="@style/IntroButtons"
            android:text="Select Reefs"
            android:layout_weight=".5"/>

    </LinearLayout>


</FrameLayout>